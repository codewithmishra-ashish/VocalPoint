import { Box, Heading, Text } from "@chakra-ui/react";

const Home = () => (
  <Box p={6}>
    <Heading>Welcome to VocalPoint</Heading>
    <Text mt={4}>Your platform to collaborate and share ideas 🚀</Text>
  </Box>
);

export default Home;
