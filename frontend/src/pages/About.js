import { Box, Heading, Text } from "@chakra-ui/react";

const About = () => (
  <Box p={6}>
    <Heading>About VocalPoint</Heading>
    <Text mt={4}>
      VocalPoint is an interactive platform where users can share, collaborate,
      and grow together.
    </Text>
  </Box>
);

export default About;
